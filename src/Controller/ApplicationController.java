package controller;

import turtle.Turtle;
import view.TurtleView;
import model.Model;
import mvcutils.AbstractController;

public class ApplicationController extends AbstractController {

/*	public void showPersonView(Object selectedValue) {
	 //construct the MVC binding
	 Model m = (Model)selectedValue;
	 TurtleController tController = new TurtleController();
	 TurtleView tView = new TurtleView(tController);
	 tController.addModel(m);
	 tController.addView(tView);
	
	
	 //show the view
	 tView.setVisible(true);
	
	 //display initial properties on view by firing the property actions
	 //t.fireInitialProperties();
	}*/
}